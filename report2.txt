建模: 
用矩阵来存，arr[i][j]用来存从第i个路口到第j个路口的长度。这样存下n个路口和m个街道作为结点和边。而后添加小哥、商户、顾客
的位置，分别新增为第n+1到第n+3个结点，并且依照题意添加其到所在街道端点的边。特别的，如果有任何两者在同
一条街道上，则在两者之间再添加一条边。如此构建出所需要的图
算法：即dijkstra算法，用该算法分别计算第n+1个结点到第n+2个结点的最短距离和第n+2个结点到第n+3个结点的最短
距离，求和即为所求。